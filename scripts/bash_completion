#!/bin/bash
_wodoo_completion() {
    local IFS=$'
'
    COMPREPLY=( $( env COMP_WORDS="${COMP_WORDS[*]}" \
                   COMP_CWORD=$COMP_CWORD \
                   _WODOO_COMPLETE=complete_bash $1 ) )
    return 0
}
